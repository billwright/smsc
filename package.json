{
  "name": "smsc",
  "version": "0.0.1",
  "dependencies": {
    "cookie-parser": "^1.4.6",
    "dotenv": "^16.3.1",
    "ejs": "^3.1.9",
    "express": "^4.18.2",
    "morgan": "^1.10.0",
    "pg": "^8.11.0",
    "pg-promise": "^11.5.0"
  },
  "devDependencies": {
    "chai": "^4.3.7",
    "chai-http": "^4.4.0",
    "mocha": "^10.2.0",
    "nodemon": "^2.0.22"
  },
  "scripts": {
    "test": "mocha --recursive ./tests",
    "testw": "mocha --recursive ./tests --watch ./modules",
    "start": "echo 'Copying proper .env file...' && cp environments/.env.smsc ./.env && nodemon index.js",
    "sLab": "nodemon src/main/nodeRestPostgresLab/index.js",
    "startDatabase": "echo 'TODO...'",
    "setLabEnv": "cp environments/.env.nodeRestPostgres ./.env && cat .env",
    "setEnv": "cp environments/.env.smsc ./.env && cat .env",
    "loadHikingDB": "cd db/labs && /Applications/flyway-9.20.0/flyway migrate && cd ."
  }
}
